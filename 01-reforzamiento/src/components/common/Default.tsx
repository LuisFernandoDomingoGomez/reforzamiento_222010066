import React from 'react';

const Default: React.FC = () => {
  // Implementación del componente predeterminado
  return (
    <div>
      {/* Contenido del componente predeterminado */}
    </div>
  );
};

export default Default;